#!/usr/bin/env ruby

require 'bundler'
Bundler.setup

lib = File.expand_path('../lib', __FILE__)
$LOAD_PATH.unshift(lib) unless $LOAD_PATH.include?(lib)

require 'rippler'

command = ARGV.empty? ? ['account_info'] : ARGV

Rippler.send *command